---
title: "Post title goes here"
description: "One-sentence summary shown in the log listing and meta tags."
date: 2026-01-01
tags: ["tag-one", "tag-two"]
draft: true
---

This file is a template — `draft: true` keeps it out of the build entirely.

To publish a new post, copy this file, rename it (the filename becomes the
URL slug, e.g. `my-first-agent.md` → `/blog/my-first-agent`), fill in the
frontmatter above, remove the `draft` line (or set it to `false`), and write
in normal markdown below.

## Headings look like this

Regular paragraphs, **bold**, *italics*, [links](https://example.com), and:

```python
def code_blocks(also):
    return "work fine"
```

> Blockquotes too.

Commit the file to `src/content/blog/` and the site rebuilds automatically.
