// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // TODO: after deploying, set this to your real URL (e.g. https://sankalp.vercel.app)
  site: 'https://example.com',
});
